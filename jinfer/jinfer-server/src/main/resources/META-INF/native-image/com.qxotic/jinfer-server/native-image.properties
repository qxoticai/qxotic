# jinfer-server - module requirement for the HTTP transport.
#
# The server uses com.sun.net.httpserver; jdk.httpserver is not in the default module set for a
# classpath build, so native-image needs it resolvable when scanning these classes.
Args = --add-modules jdk.httpserver
