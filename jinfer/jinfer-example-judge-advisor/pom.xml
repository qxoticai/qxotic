<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.qxotic</groupId>
    <artifactId>jinfer-parent</artifactId>
    <version>0.2.0</version>
  </parent>

  <artifactId>jinfer-example-judge-advisor</artifactId>
  <packaging>jar</packaging>

  <name>Jinfer Judge Advisor Example</name>
  <description>Demo: a remote OpenAI-compatible generator with a local in-JVM Jinfer judge. A
    self-refine advisor loops generation until the judge's grammar-pinned verdict passes. Not
    published to Maven Central.</description>

  <properties>
    <maven.deploy.skip>true</maven.deploy.skip>
  </properties>

  <!-- Demo: remote generator (Kimi, OpenAI-compatible) + LOCAL in-JVM judge (jinfer).
       A self-refine advisor loops generation until the judge's grammar-pinned verdict
       passes; the judge's rubric is prefilled once via jinfer's cached prompts. The
       official spring-ai-examples evaluation-recursive-advisor-demo pattern, hardened:
       the judge's JSON verdict is enforced token-level (GBNF), so it always parses and
       the rating is always 1-4. Run: KIMI_API_KEY=... mvn spring-boot:run -->
  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.ai</groupId>
        <artifactId>spring-ai-bom</artifactId>
        <version>${spring-ai.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>com.qxotic</groupId>
      <artifactId>jinfer-spring-ai-spring-boot-starter</artifactId>
    </dependency>
    <dependency>
      <groupId>com.qxotic</groupId>
      <artifactId>jinfer-lfm2</artifactId>
    </dependency>
    <dependency>
      <groupId>com.qxotic</groupId>
      <artifactId>json</artifactId>
    </dependency>
    <!-- the remote generator: Kimi's OpenAI-compatible endpoint -->
    <dependency>
      <groupId>org.springframework.ai</groupId>
      <artifactId>spring-ai-starter-model-openai</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- shared model fixtures (paths relative to the checkout; scripts/download-models.sh) -->
    <dependency>
      <groupId>com.qxotic</groupId>
      <artifactId>jinfer-testkit</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- version + jinfer JVM flags managed by the parent -->
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
